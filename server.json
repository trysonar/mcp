{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "app.trysonar/sonar",
  "description": "ASO tools for AI agents: keyword research, rank tracking, competitor analysis (iOS & Android).",
  "version": "0.7.0",
  "repository": {
    "url": "https://github.com/trysonar/mcp",
    "source": "github"
  },
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://trysonar.app/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Bearer <Sonar API key (aso_...)>. Create one at https://trysonar.app/developers",
          "isRequired": true,
          "isSecret": true
        }
      ]
    }
  ],
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@sonarapp/mcp",
      "version": "0.7.0",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        {
          "name": "SONAR_API_KEY",
          "description": "Your Sonar API key (aso_...). Create one at https://trysonar.app",
          "format": "string",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "SONAR_API_URL",
          "description": "API base URL. Defaults to https://trysonar.app.",
          "format": "string",
          "isRequired": false,
          "default": "https://trysonar.app"
        }
      ]
    }
  ]
}
